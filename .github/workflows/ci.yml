name: CI Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  ci_logic:
    name: Build & Test Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: TypeScript type check
        run: npx tsc --noEmit

      # Step 6: এখানে docker-compose এর বদলে docker compose ব্যবহার করুন
      - name: Build with Docker Compose
        run: docker compose build

      # Step 7: এখানেও docker-compose এর বদলে docker compose দিন
      - name: Run Unit Tests
        run: docker compose run -e CI=true app npm test

      # Step 8: Cleanup স্টেপেও একই পরিবর্তন
      - name: Stop Containers
        if: always()
        run: docker compose down